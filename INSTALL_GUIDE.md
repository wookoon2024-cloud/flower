# [설치 및 설정 가이드] 범정부 AI API 기반 데스크톱 플로팅 AI 반려화분 위젯

---

## 1. 프로그램 개요
본 프로그램은 **행정·업무망(Windows 10/11) 환경**에서 직원의 업무 피로도를 완화하고 친근하게 AI와 상호작용할 수 있도록 제작된 **데스크톱 투명 플로팅 다마고치 반려화분 위젯**입니다.

* **운영체제:** Windows 10 / 11 (32bit / 64bit)
* **네트워크 환경:** 행정망/업무망(망분리 폐쇄망 또는 인터넷망 지원)
* **실행 형태:** 별도 Python 설치 없이 실행 가능한 독립 실행 파일 (`AICompanionPlant.exe`)

---

## 2. 주요 기능 및 사용 방법

### 1) 화면 상주 및 조작
* **투명 플로팅 창:** 작업표시줄 및 다른 창 위에서 항상 상주하며 귀여운 반려식물이 함께합니다.
* **위치 이동 (드래그):** 화분 또는 위젯 영역을 **마우스 좌클릭 후 드래그**하여 모니터 원하는 위치로 이동할 수 있습니다. (위치 자동 기억)
* **컨텍스트 메뉴 (우클릭):** 위젯 어디서나 **마우스 우클릭** 시 상세 메뉴(대화, 물주기, 햇빛, 최상위 고정 토글, 환경설정, 종료)가 나타납니다.

### 2) 다마고치 육성 시스템
* **💧 물주기:** 하단 `💧 물주기` 버튼 클릭 시 수분 +25 회복
* **☀️ 햇빛쬐기:** 하단 `☀️ 햇빛` 버튼 클릭 시 햇빛 +25 회복
* **💕 쓰다듬기:** 화분 캐릭터를 **마우스 좌클릭**하여 쓰다듬으면 하트 이펙트와 함께 애정도 상승
* **🌱 4단계 진화 시스템:**
  * **1단계 (새싹 🌱):** 시작 단계의 귀여운 새싹
  * **2단계 (줄기 🌿):** 잎사귀가 자란 줄기 단계
  * **3단계 (꽃봉오리 🪴):** 꽃을 피우기 직전의 봉오리 단계
  * **4단계 (활짝 핀 꽃 🌸):** 만개한 꽃과 반짝이는 이펙트
* **⏳ 자연 감쇠 및 오프라인 시간 보정:**
  * 30분마다 수분/햇빛 수치가 서서히 감소합니다.
  * PC를 끄고 퇴근한 후 다음 날 출근하여 프로그램을 켜면, 미접속 시간에 비례하여 수치가 감쇠 적용됩니다.
* **⚠️ 긴급 알림:** 수분 또는 햇빛이 20% 이하로 떨어지면 화분이 말풍선으로 목마름/부족 상태를 알립니다.

---

## 3. 범정부 AI (dev.ai.go.kr) API 설정 방법

### 1) 환경설정 창 열기
* 위젯 하단의 **`⚙️` 아이콘**을 누르거나, 화분을 **우클릭 $\rightarrow$ [⚙️ 환경 설정]**을 선택합니다.

### 2) 설정 항목 입력
1. **🌐 AI API 설정 탭:**
   * **API 엔드포인트:** `https://dev.ai.go.kr/api/v1/chat/completions` (또는 지정된 공통기반 엔드포인트 URL)
   * **API Key:** 범정부 초거대 AI 공통기반 포털에서 발급받은 인증키 입력
   * **모델명:** `gov-gpt-4o` (또는 기관에 배정된 모델명)
   * **요청 타임아웃:** 기본 `5초` 권장
   * **SSL 인증서 검증 건너뛰기:** ✅ **체크 권장** (행정망/업무망 내부 사설 인증서나 SSL 가로채기 보안 장비 환경에서도 정상 통신 가능)
2. **🌱 화분/위젯 설정 탭:**
   * **화분 이름:** 원하는 반려화분 이름 (기본값: `초록이`)
   * **사용자 호칭:** AI가 사용자를 부를 호칭 (기본값: `김주무관`, `공직자님` 등)
   * **항상 위에 고정 (Always On Top):** 작업 중 항상 위젯을 보이게 할지 여부
3. **[저장하기 💾]** 버튼을 클릭하면 설정이 즉시 반영됩니다.

---

## 4. 오프라인 / 망분리 폐쇄망 Fallback 동작
* 만약 인터넷이 연결되지 않거나 API 키가 설정되지 않은 상태, 또는 5초 내 응답이 없는 경우에도 프로그램이 중단되지 않습니다.
* 반려식물 자체 내장 **로컬 인텔리전트 Fallback 대사 엔진**이 작동하여, 사용자의 말(피로, 응원, 인사, 수분 상태 등)을 분석해 따뜻하고 귀여운 맞춤형 위로 대사를 출력합니다.

---

## 5. 파일 구성 및 데이터 관리
* `AICompanionPlant.exe`: 독립 실행 바이너리
* `config.json`: 사용자 설정 파일 (API Key, 사용자 이름 등)
* `plant_data.db`: SQLite3 데이터베이스 파일 (화분 상태, 최근 대화 기록 영속화)
* `assets/`: 화분 그래픽 및 아이콘 이미지 폴더 (사용자 커스텀 이미지로 교체 가능)

> **팁:** 화분을 처음부터 다시 키우고 싶거나 대화 기록을 지우고 싶으실 때는 **[환경설정] $\rightarrow$ [💾 데이터 관리]** 탭에서 언제든지 초기화할 수 있습니다.
